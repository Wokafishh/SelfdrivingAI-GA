# SelfdrivingAI-GA
Python code for self driving models. Three different models will be trained with different algorithmns.

.venv innehåller BARA modules för CPU-träning!

Använder Pytorch och Webots.
